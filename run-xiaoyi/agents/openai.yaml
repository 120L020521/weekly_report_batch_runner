interface:
  display_name: "Run XiaoYi"
  short_description: "统一路由小艺文件整理、周报、WorkspaceBench 与诊断流程"
  default_prompt: "Use $run-xiaoyi to route this XiaoYi request and stop after the last stage explicitly requested by the user."
policy:
  allow_implicit_invocation: true
